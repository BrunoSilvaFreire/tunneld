apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# One-shot install bundle: CRDs, RBAC, controller Deployment, Mode A agent
# DaemonSet. For Mode B, replace ../agent with a small overlay.
namespace: tunneld-system
resources:
  - ../crd
  - ../rbac
  - ../manager
  - ../agent
